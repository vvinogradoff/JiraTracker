<Window x:Class="UpworkJiraTracker.View.WorklogInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:UpworkJiraTracker.ViewModel"
        mc:Ignorable="d"
        Title="Log Work"
        Width="350"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Loaded="Window_Loaded"
        PreviewKeyDown="Window_PreviewKeyDown"
		d:DataContext="{d:DesignInstance Type=vm:WorklogInputViewModel, IsDesignTimeCreatable=True}">

    <Border Background="White"
            BorderBrush="#CCCCCC"
            BorderThickness="1"
            CornerRadius="4"
            Padding="12,10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10"
                              ShadowDepth="2"
                              Opacity="0.3"
                              Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Description field -->
            <TextBox x:Name="DescriptionTextBox"
                     Grid.Column="0"
                     Text="{Binding WorkDescription, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FlatTextBoxStyle}"
                     ToolTip="Describe the progress (optional)"
                     Margin="0,0,8,0"/>

            <!-- ETA field -->
            <TextBox x:Name="EtaTextBox"
                     Grid.Column="1"
                     Text="{Binding EtaText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource FlatTextBoxStyle}"
                     ToolTip="Remaining estimate in hours (optional)"
                     Margin="0,0,8,0"/>

            <!-- Submit button -->
            <Button x:Name="SubmitButton"
                    Grid.Column="2"
                    Content="Log"
                    Width="50"
                    Height="22"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Padding="8,2"
                    Command="{Binding SubmitCommand}"/>
        </Grid>
    </Border>
</Window>
